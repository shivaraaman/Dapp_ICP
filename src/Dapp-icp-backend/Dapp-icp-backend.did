type Student = record {
  name: text;
  total: nat32;
  subjects: nat32;
  average: float32;
  grade: text;
};

service : {
  add_student: (text, nat32, nat32) -> ();
  get_students: () -> (vec Student);
};
